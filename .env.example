# Base URL of the DCUOBot census API this app talks to.
# Optional — defaults to the production API (https://dcuo.bot/api/v1/census) if unset.
# Set this to point the app at a local/staging backend during development.
VITE_API_BASE_URL=https://dcuo.bot/api/v1/census
